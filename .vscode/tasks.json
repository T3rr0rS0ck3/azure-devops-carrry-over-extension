{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Create Azure DevOps Extension",
            "type": "shell",
            "command": "Remove-Item -Path '*.vsix' -Force -ErrorAction SilentlyContinue; tfx extension create --manifest-globs vss-extension.json --rev-version",
            "problemMatcher": []
        }
    ]
}